[project]
name = "housing-sim-jp"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
housing-sim = "housing_sim_jp.cli:main"
scenario-compare = "housing_sim_jp.scenario_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
